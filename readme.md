The two PDF files explain how to update the firmware of the **C6** chip using the **P4** chip.
One file uses PlatformIO. The other uses ESP-IDF.
Both provide step-by-step instructions.

The file named “host_performs_slave_ota” includes the code for the ESP-IDF method.
